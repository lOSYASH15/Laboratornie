/*При статическом варианте метод можно вызывать при компиляции кода, происходит это при помощи перегрузки методов. 
У них будет одинаковое название, но разная функциональность (тип возвращаемых данных, свойств). 
В таком случае Java позволяет пользователю свободно определять функциям идентичные названия, если они будут отличны по типу и параметрам.*/
public class Add {
  void sum(int x, int y) {
    int z = x + y;
    System.out.println("Сумма двух чисел:" +z);
  }
  void sum(int x, int y, int i) {
    int z = x + y + i;
    System.out.println("Сумма трех чисел: " +z);
  }
  public static void main(String[] args) {
    Add myAdd = new Add();
    myAdd.sum(5, 11);
    myAdd.sum(1, 8, 11);
  }
}
